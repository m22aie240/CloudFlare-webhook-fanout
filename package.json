# ---------------------------------------------------------------------
// File: package.json
// ---------------------------------------------------------------------
{
  "name": "cf-webhook-fanout",
  "version": "0.1.0",
  "type": "module",
  "devDependencies": {
    "wrangler": "^3.9.0",
    "typescript": "^5.5.0",
    "vitest": "^1.5.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  }
}
